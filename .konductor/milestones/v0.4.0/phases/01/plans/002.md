+++
phase = "01"
plan = 2
title = "MCP Prompt Tests"
wave = 2
depends_on = [1]
type = "tdd"
autonomous = true
requirements = ["REQ-01", "REQ-08"]
files_modified = ["konductor-cli/tests/mcp_prompts.rs"]

[must_haves]
truths = ["All 9 MCP prompts are listed via list_all_prompts", "No-arg prompts (k-init, k-exec, k-verify, k-ship, k-next, k-status, k-map) return correct message text", "Arg prompts (k-plan, k-discuss) accept phase argument and include it in message", "Each test uses isolated temp directory"]
artifacts = ["konductor-cli/tests/mcp_prompts.rs"]
key_links = ["cargo test --test mcp_prompts passes", "Reuses test harness pattern from mcp_tools.rs"]
+++

# Plan 02: MCP Prompt Tests

## Goal
Test all 9 MCP prompts via the rmcp client, validating message content and argument handling.

## Dependencies

Depends on Plan 01 for the test harness pattern (spawn binary, connect client, temp dir setup).

## Tasks

### Task 1: Write prompt listing and no-arg prompt tests

- **files:** `konductor-cli/tests/mcp_prompts.rs`
- **action:** Create integration test file. Reuse the harness pattern from mcp_tools.rs. Write test that lists all prompts and asserts 9 are returned. Write tests for no-arg prompts: k-init, k-exec, k-verify, k-ship, k-next, k-status, k-map — each should return a message containing the expected trigger text (e.g., k-init message contains "Initialize").
- **verify:** `cargo test --test mcp_prompts` passes
- **done:** No-arg prompts tested

### Task 2: Write arg prompt tests

- **files:** `konductor-cli/tests/mcp_prompts.rs`
- **action:** Write tests for k-plan and k-discuss prompts with phase argument. Verify the returned message contains the phase number. Test k-plan with phase "01" — message should contain "01".
- **verify:** `cargo test --test mcp_prompts` passes
- **done:** All 9 prompts tested
