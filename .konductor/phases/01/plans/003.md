+++
phase = "01"
plan = 3
title = "Full Plan Frontmatter Parser"
wave = 1
depends_on = []
type = "tdd"
autonomous = true
requirements = ["REQ-04", "REQ-02"]
files_modified = ["konductor-cli/src/mcp.rs"]

[must_haves]
truths = ["Frontmatter parser extracts all documented fields: phase, plan, wave, title, depends_on, type, autonomous, requirements, files_modified, must_haves", "Parser handles missing optional fields gracefully", "Parser returns structured PlanFrontmatter type", "plans_list tool uses full frontmatter data"]
artifacts = ["konductor-cli/src/mcp.rs"]
key_links = ["PlanFrontmatter struct used by plans_list tool", "cargo test mcp:: passes all tests"]
+++

# Plan 03: Full Plan Frontmatter Parser

## Goal
Rewrite the plan frontmatter parser to extract all documented fields using proper TOML parsing, and update the plans_list tool to use the full data.

## Tasks

### Task 1: Write frontmatter parser tests

- **files:** `konductor-cli/src/mcp.rs`
- **action:** Add tests for `parse_plan_frontmatter()` with: complete frontmatter (all fields), minimal frontmatter (only wave), missing frontmatter delimiters, nested must_haves table, depends_on array, requirements array
- **verify:** `cargo test mcp::` — tests exist and fail (current parser only returns wave/title)
- **done:** Test cases define the expected behavior

### Task 2: Define PlanFrontmatter struct and rewrite parser

- **files:** `konductor-cli/src/mcp.rs`
- **action:** Create `PlanFrontmatter` struct with all fields (phase, plan, wave, title, depends_on, type, autonomous, requirements, files_modified, must_haves). Rewrite `parse_plan_frontmatter()` to extract content between `+++` delimiters and parse with `toml::from_str()`. Return `PlanFrontmatter` instead of `(i64, String)`.
- **verify:** `cargo test mcp::` passes
- **done:** Parser handles all documented frontmatter fields

### Task 3: Update plans_list tool to use full frontmatter

- **files:** `konductor-cli/src/mcp.rs`
- **action:** Update `plans_list` tool to use `PlanFrontmatter` struct. Include depends_on, type, requirements, and completion status in the JSON output.
- **verify:** `cargo test mcp::` passes, `cargo check` passes
- **done:** plans_list returns enriched plan data
