# Konductor Benchmark Baseline
# Fill in after first successful benchmark run:
#   KONDUCTOR_E2E=1 bash tests/e2e/benchmark.sh
# Then copy last-results.toml values here.

# Per-step timing (seconds) — upper bounds for regression detection
init_time_s = 120
plan_time_s = 120
exec_time_s = 300
verify_time_s = 120

# Quality metrics
plan_count_min = 1
gap_count_max = 0
verification_status = "ok"
